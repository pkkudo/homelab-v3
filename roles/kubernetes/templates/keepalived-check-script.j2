#!/bin/sh

errorExit() {
	echo "*** $*" 1>&2
	exit 1
}

curl -sfk --max-time 2 https://localhost:{{ kube_endpoint_port }}/healthz -o /dev/null || errorExit "Error GET https://localhost:{{ kube_endpoint_port }}/healthz"
