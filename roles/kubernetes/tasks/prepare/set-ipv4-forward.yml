---
- name: Set ipv4 forwarding kernel parameter
  ansible.builtin.blockinfile:
    path: "/etc/sysctl.d/k8s.conf"
    block: |
      net.ipv4.ip_forward = 1
    create: true
    mode: "0644"
  become: true
  notify:
    - sysctl reload
